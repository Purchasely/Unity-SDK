plugins {
    id 'com.android.library'
}

android {
    namespace 'com.purchasely.unity'
    compileSdk 32

    defaultConfig {
        minSdk 21
        targetSdk 32
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation 'io.purchasely:core:1.1.0-alpha02'
    implementation 'io.purchasely:player:1.1.0-alpha02'
    implementation 'io.purchasely:google-play:1.1.0-alpha02'
}

task copyAarToUnity {
    doLast {
        project.copy {
            from "$buildDir/outputs/aar"
            include '*release.aar'
            println "Copying into [$rootProject.ext.libraryFolder]"
            into rootProject.ext.libraryFolder
        }
    }
}

tasks.build.finalizedBy copyAarToUnity